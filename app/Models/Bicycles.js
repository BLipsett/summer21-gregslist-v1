export class Bicycle {
  constructor(type, brand, price, description, img) {
    (this.type = type),
      (this.brand = brand),
      (this.price = price),
      (this.description = description),
      (this.img = img);
  }
}
